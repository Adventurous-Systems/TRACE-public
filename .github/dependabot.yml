version: 2
updates:
  # Keep SHA-pinned GitHub Actions up to date (reads the `# vX.Y.Z` comments)
  - package-ecosystem: github-actions
    directory: '/'
    schedule:
      interval: weekly
    cooldown:
      default-days: 7
